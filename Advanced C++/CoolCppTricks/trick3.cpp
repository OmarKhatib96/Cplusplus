

#include <iostream>

using namespace std;
int function(void);


#define WHOLE(X) begin(X),end(X)

const int INF=0x3f3f3f3f;

sort(WHOLE(my_container))


#define AB(x,y) do { a(x);b(y);} while(0)
#define ARRAY_SZ(x) sizeof(X)/sizeof(*(X))//To find the number of elements in an array using a macro like

//__FUNCTION__.//To get the name of the current function  
//#pragma startup function

int count=10;
/*
void f(void)
{

    int count=0;
    cout<<count;//prints 0
    cout<<::count;//prints 10
}*/

//To start the execution of the program from my defined function and not from the void main or int main function


int function(void)
{

    std::cout<<"Starti executing...";
}

//strerror(errno) we can convert it to string readable to know the error we got


//usage of move
#include <vector>

vector<int> v={1,2,3,4};
vector<int> w=move(v);//we can use move function to just move container, not to copy it all

//To clean up memory spaces that are no longer used.
bool function_t(int val){

    char *msg=new char[30];
    if(val==VALID){
        //do processing....
    }
    else{
        delete [] msg;
        return false;
    }

}

//use printf instead of cout for speed


int numberOfSetBits=__builtin_popcount;//to count



//use macros for containers

#pragma exit

int main(void)
{
    cout<<"executing the main function";
    return 1;
}



